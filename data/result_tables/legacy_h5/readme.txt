The hdf5 tables in this folder are created by the python files in /code/data_compilation
We have since switched from hdf5 to json format, however, the h5 files are still being read in in "Combine_and_process_data.py", 
the latter completes them to include newer data columns and combines them into the master catalog "post_mt_systems.json"